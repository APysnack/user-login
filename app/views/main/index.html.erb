<h1>main</h1>

<% if Current.user %>
    <h1>Logged in as <%= Current.user.email %></h1>
    <%= button_to "Sign out", sign_out_path, method: :delete %>
<% else %>
    <%= button_to "Sign in", sign_in_path %>
    <%= button_to "Sign Up", sign_up_path, method: :get %>
<% end %>